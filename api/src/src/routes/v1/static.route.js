const express = require("express");

const router = express.Router();

// router
//   .route("/thumbnails/:filename")
//   .get(auth("getThumbnails"), validate(staticValidation.getFile), (req, res) => {
//     res.sendFile(path.join(config.static.thumbnailsFolder, req.params.filename));
//   });

module.exports = router;
